#include <stdio.h>
#include <stdlib.h>

typedef struct nodes {
    int color;
    int key;
    struct node *left;
    struct node *right;
    struct node *parent;
} node;

int main() {
    return 0;
}
