#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int N, M;
    int C[50];
    int count = 0;
    cin >> N >> M;
    for (int i = 0; i<M; i++) {
        cin >> C[i];
    }
    cout << count << endl;
    return 0;
}

